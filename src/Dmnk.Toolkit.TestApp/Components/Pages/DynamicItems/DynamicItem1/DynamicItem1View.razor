@inherits MvvmComponentBase<DynamicItem1ViewModel>

<h3>DynamicItem1View</h3>

<p>Counter: @Vm.Counter (Injected as scoped, so will be refreshed on full page load (circuit))</p>

<FluentButton OnClick="@(() => Vm.Counter++)">Increment</FluentButton>
