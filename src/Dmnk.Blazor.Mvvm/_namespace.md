---
uid: Dmnk.Blazor.Mvvm
---

This library provides some base types to inherit from in a Blazor application, to implement the MVVM pattern. It makes
no assumptions about the implementation of the ViewModel, only that it must implement `INotifyPropertyChanged` and
that commands implement `ICommand`.
